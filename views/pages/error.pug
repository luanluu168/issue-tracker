extends layout.pug
    
block content
    //- Intro
    div(class='container-fluid pt-3')
        div(class='d-flex flex-column justify-content-center align-items-center')
            h1(class='big-heading-text text-primary') !{code}
            h3(class='describe-app-text') !{error}
            h5(class='mt-3 text-white') !{detail}
            a(href='/' name="home" class='start-button mt-4') Home